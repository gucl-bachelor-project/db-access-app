version: "2.1"

volumes:
  nodemodules_db_access_admin:
    external: true

services:
  base:
    image: node:alpine
    volumes:
      - nodemodules_db_access_admin:/app/node_modules
      - .:/app
    working_dir: /app

  install:
    extends:
      service: base
    command: npm i
